[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crater_can"
version = "0.1.0"
description = "Drivers for the team testbench USB-CAN adapter"
dependencies = [
    "pyserial",
]

[tool.setuptools.packages.find]
where = ["src"]